#!/bin/bash
alias add="sudo pacman -S"
alias rmv="sudo pacman -Rns"
alias upd="sudo pacman -Syu"
alias yt="youtube-dl --add-metadata -i"
alias yta="youtube-dl -x -f bestaudio/best"
alias ka="killall"
alias sdn="shutdown now"
alias rbn="reboot"
alias untar="tar -zxvf"
alias myip="curl ipinfo.io/ip"
alias dez="cd  ~/TEST/deemix-pyweb && python3 server.py | brave http://127.0.0.1:6595 "
alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -v"
alias mkd="mkdir -pv"
alias v="nvim"
alias sv="sudo nvim"
alias sss="sudo systemctl start"
alias sse="sudo systemctl enable"
alias ssd="sudo systemctl disable"
alias lspkgs="sudo pacman -Q | v pkgs"
alias als="v .config/aliasrc"
alias alk="source .bashrc"
alias cl="clear"
alias lm="ls -t -1"
alias lt="ls --human-readable --size -1 -S --classify"
alias cnv="ffmpeg -i"
alias g="git clone"
